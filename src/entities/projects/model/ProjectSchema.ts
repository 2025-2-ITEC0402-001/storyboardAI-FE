export interface ProjectSchema {
    id: string;
    title: string;
    description: string;
    ownerId: string;
    canvas: string;
    thumbnail: string;
    createdAt: string;
    updatedAt: string;
}
